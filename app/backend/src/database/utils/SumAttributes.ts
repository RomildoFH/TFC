// import ITeamBoard from '../interfaces/ITeamBoard';

// export default class SumAtrributes {
//   // public static SumAll(board: ITeamBoard[]) {
//   //   const leaderBoard: ITeamBoard[] = [];
//   //   for (let index = 0; index < board.length; index += 1) {
//   //     const current = board[index];
//   //     if (!leaderBoard.some((boardTeam) => boardTeam.name === current.name)) {
//   //       for (let count = 0; count < board.length; count += 1) {
//   //         const next = board[count];
//   //         if (current.name === next.name && index !== count) {
//   //           current.totalPoints += next.totalPoints;
//   //           current.totalGames += next.totalGames;
//   //           current.totalVictories += next.totalVictories;
//   //           current.totalDraws += next.totalDraws;
//   //           current.totalLosses += next.totalLosses;
//   //           current.goalsFavor += next.goalsFavor;
//   //           current.goalsOwn += next.goalsOwn;
//   //           current.goalsBalance += next.goalsBalance;
//   //           current.efficiency += next.efficiency;
//   //         }
//   //       }
//   //       leaderBoard.push(current);
//   //     }
//   //   }
//   //   return leaderBoard;
//   // }

//   // public static SumWithReduce(board: ITeamBoard[]) {
//   //   board.reduce()
//   // }
// }
